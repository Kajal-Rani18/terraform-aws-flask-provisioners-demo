# terraform-aws-flask-provisioners-demo
Terraform AWS Flask Deployment — An end-to-end Terraform project that provisions AWS infrastructure and deploys a simple Python Flask application to an EC2 instance using Terraform provisioners and SSH connection blocks.
